import 'package:bookoo2/models/Category.dart';
import 'package:flutter/material.dart';
import 'package:flutter_test/flutter_test.dart';

import 'package:bookoo2/shared/category_item.dart' show CategoryItem;
import '../test_utils.dart' show TestUtils;

void main() {
  testWidgets("Button", (WidgetTester tester) async{
    // await tester.pumpWidget(TestUtils.makeTestableWidget(child: CategoryItem(
    //   category: Category(),
    // )));
    // await tester.pumpAndSettle();

    // var findByText = find.byType(Text);
    // expect(findByText.evaluate().isEmpty, false);

    // expect(find.text('TextTest'), findsOneWidget);
  });
}
